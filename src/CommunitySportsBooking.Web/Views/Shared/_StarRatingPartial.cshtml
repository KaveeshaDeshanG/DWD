@model byte
@for (var i = 1; i <= 5; i++)
{
    <i class="bi @(i <= Model ? "bi-star-fill" : "bi-star") rating-star-display@(i <= Model ? "" : " empty")" aria-hidden="true"></i>
}
<span class="visually-hidden">@Model out of 5 stars</span>
